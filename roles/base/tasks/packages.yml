--- # base packages:

- name: base role - packages | apt update && apt dist-upgrade *MAY TAKE A LONG TIME*
  apt:
    update_cache: True
    upgrade: dist

# keep alphabetically ordered
- name: base role - packages | install standard additions to the base install
  apt:
    state: latest
    pkg:
      - brasero
      - build-essential
      - clamav
      - clamav-daemon
      - curl
      - exfat-utils
      - firefox
      - fzf
      - gcc
      - gimp
      - git
      - golang
      - htop
      - jq
      - make
      - meld
      - net-tools
      - nmap
      - p7zip
      - python3-pip
      - qpdf
      - ranger
      - rtorrent
      - sshfs
      - tcpdump
      - tmux
      - tree
      - ufw
      - unrar
      - unzip
      - vim
      - virtualbox
      - vlc
      - whois

- name: base role - packages | apt autoremove & autoclean
  apt:
    autoremove: True
    autoclean: True

- name: base role - packages | enable clamav service
  systemd:
    name: clamav-daemon
    enabled: True
    state: started

- name: base role - packages | install additional pip packages
  pip:
    state: latest
    executable: pip3
    name:
      - pip # self upgrade pip first
      - glances
      - bottle
