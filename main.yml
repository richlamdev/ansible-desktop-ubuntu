---
- name: Playbook desktop start
  hosts: all
  #user:
  become: True
  become_user: root
  become_method: sudo
  gather_facts: True
  #tasks:
  #- include_tasks: tasks/main.yml

  roles:
    - base
    - aws-cli
    - brave
    #- chrome
    #- docker
    #- microsoft
    #- gh_cli
    #- hashicorp
    #- keepassxc
    #- signal-desktop
    #- vivaldi
    #- opera
    #- ufw
    - env
    - vim
    - disable-local-dns
    #- yubico
